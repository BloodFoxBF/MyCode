//const solveTask = document.querySelector('.solve');

/*const postData = fetch (url, {
    method: 'POST',
    headers: {'Content-Type': 'text/plain', 'id': 1, 'userfunction': `public static int add(int a, int b){return a+b;}`}
    }).then(function (response) {
        return response.json()
    }).then(function (data) {
        console.log('Successful', data);
    });
*/
